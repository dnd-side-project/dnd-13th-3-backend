spring:
  application:
    name: dnd-13th-3-backend

  # MySQL 데이터베이스 설정
  datasource:
    url: jdbc:mysql://localhost:3306/minu?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: 3632015jj
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA 및 Hibernate 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

  # OAuth2 Google 설정
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 781984674-ck704o05ufch72klqtmm5bovj483b7gs.apps.googleusercontent.com
            client-secret: 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
            scope: profile,email,https://www.googleapis.com/auth/youtube.readonly
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

# 서버 포트 설정
server:
  port: 8080

# 로깅 설정
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
    com.zaxxer.hikari: DEBUG

# JWT 설정
jwt:
  secret: mySecretKeyForJWTTokenGenerationThatShouldBeLongEnoughForSecurity
  access-token-validity-in-seconds: 1800
  refresh-token-validity-in-seconds: 604800